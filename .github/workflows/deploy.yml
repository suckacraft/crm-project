# GitHub Pages deployment is DISABLED.
# This CRM is configured for fully local/on-premise hosting.
# See LOCAL_HOSTING.md for setup instructions.
#
# To re-enable public deployment, restore this file from git history
# and review the security implications with your security team first.

name: Deploy to GitHub Pages (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type CONFIRM to re-enable deployment (requires security approval)"
        required: true

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment disabled
        run: |
          echo "GitHub Pages deployment is disabled for security compliance."
          echo "This CRM must be hosted on internal infrastructure only."
          exit 1
