// Version stamps used as provenance on every learning example, so the corpus
// stays interpretable and comparable across product changes. Bump deliberately.

export const APP_VERSION = "0.1.0";            // the capture app
export const EXAMPLE_SCHEMA_VERSION = 1;        // the learning-example contract
